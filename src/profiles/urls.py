from django.urls import path, include
from . import views  

urlpatterns = [
    # path('accoutns/login/', views.login, name='login'),
    # path('accounts/signup/', views.signup, name='signup'),
    # path('accoutns/signout/', views.signout, name='signout'),

]
